<% content_for :title, "Edit #{@game.name}" %>

<div class="container mx-auto px-4 max-w-4xl">
  <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6">
    <div class="mb-6">
      <h1 class="text-3xl font-bold text-gray-900 mb-2"><%= t('pages.edit.title') %></h1>
      <p class="text-gray-600"><%= t('pages.edit.subtitle') %></p>
    </div>

    <%= render 'form', cancel_path: @game, submit_text: "Update Game" %>
  </div>
</div>
