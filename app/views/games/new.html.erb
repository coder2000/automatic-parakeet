<% content_for :title, "Upload New Game" %>

<div class="container mx-auto px-4 max-w-4xl">
  <div class="bg-[#162737] rounded-lg shadow-sm border border-[#393a3a] p-6">
    <div class="mb-6">
      <h1 class="text-3xl font-bold text-white mb-2"><%= t('pages.upload.title') %></h1>
      <p class="text-gray-300"><%= t('pages.upload.subtitle') %></p>
    </div>

    <%= render 'form', cancel_path: games_path, submit_text: "Upload Game" %>
  </div>
</div>
